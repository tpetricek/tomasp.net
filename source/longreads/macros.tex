% Shared macros for all long reads. Only put one here if tools/latex.fs handles it too -
% the converter matches command names and does not expand \newcommand.

\newcommand{\figref}[1]{Figure~\ref{#1}}
