# Copy to ".env" and fill in. Only "build.sh calendar" needs these; generating the
# site does not touch R2. Create the token under R2 > Manage API tokens, scoped to
# "Object Read & Write" on the single bucket.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
